node_modules
dist
build
.turbo
.env
.env.local
.env.*.local

# Locally-generated TOTP secrets for the dev-only demo-seeding/screenshot
# scripts (apps/web/demo-auth.cjs, seed-demo*.cjs, tour.cjs) — regenerable
# fixture material, not something that belongs in git history even for a
# throwaway demo company.
apps/web/.demo-secrets.json
*.log
.DS_Store
apps/web/dist
apps/api/dist
apps/api/generated
coverage
*.tsbuildinfo
storage-data
apps/api/storage-data

# tsconfig.node.json's composite build output for vite.config.ts (see that
# file's own comment) — was previously un-set and emitted vite.config.js/
# .d.ts checked into git right beside vite.config.ts, which is what made a
# real edit to vite.config.ts silently do nothing (Vite prefers a
# same-directory .js config over .ts).
apps/web/.tsbuild-node
